{
  "name": "voice-agent-app",
  "version": "0.1.0",
  "private": true,
  "description": "Siri-style voice assistant for Android, backed by a local LLM and a custom backend.",
  "scripts": {
    "dev": "cd angular-app && npm start",
    "build:web": "cd angular-app && npm run build",
    "sync": "./scripts/sync-www.sh",
    "build": "npm run build:web && npm run sync",
    "cordova:android": "npm run build && cd cordova-app && npx cordova platform add android && npx cordova run android"
  }
}
